#include"main.h"
/**
* mul - multiplies two numbers
* @a: first number to multipy
* @b: second number to multiply
*
* Return: 0 always
*/

int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
